{
  "name": "bob-printr",
  "version": "1.0.0",
  "description": "Command line application to print code to pdf",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/mocha --require @babel/register tests/ ",
    "build:prod": "cross-env NODE_ENV=production npx babel src --out-dir lib",
    "start:prod": "npm run build:prod && node ./lib/index.js",
    "deploy": "npm version patch && npm run build:prod && npm publish . --access public && git add . && git commit -m \"version bump\" && git push"
  },
  "repository": {
    "type": "git",
    "url": "git@scm.velor.ca:calma/codeprintr.git"
  },
  "bin": {
    "bobp": "./lib/index.js"
  },
  "author": "Glenn Hall",
  "license": "ISC",
  "keywords": [
    "Collège d'Alma",
    "Correction",
    "Informatique",
    "Note étudiants",
    "Export to pdf"
  ],
  "dependencies": {
    "jsdom": "^15.2.1",
    "prismjs": "^1.17.1",
    "request-promise-native": "^1.0.8",
    "yargs": "^15.0.2"
  }
}
